port = 8080
timeout = 1000 ;timeout in milliseconds
command_output = 0 ;options: 0: false, 1: true, 2: logs
command_output_logs_path = ./logs/logging.txt
;You can rename the file and its extension
;Make sure to specify a filename and not provide path to a directory alone
;Logging works fine with static and non-TUI commands
;But it is unpredictable with dynamic and TUI commands